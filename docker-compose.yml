

services:
  db:
    image: postgres:17.2
    shm_size: 128mb
    # pick a distinct port
    ports:
      - "5433:5432"
    environment:
      POSTGRES_PASSWORD: postgres
    env_file:
      - .env

